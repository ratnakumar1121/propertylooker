import React from 'react';
import ReactDOM from 'react-dom/client';
// import './index.css'; // Remove or create this file if needed
import App from './App';
// import reportWebVitals from './reportWebVitals'; // Remove or create this file if needed

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

// reportWebVitals(); // Remove or comment out this line if not using reportWebVitals
